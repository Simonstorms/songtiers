import { Track } from "@/components/SpotifySearch";

export const safeSong = (song: Track) => {
    // Handle the song click here. You have access to the song information.
    console.log(song); // For example, log the song information.
};
